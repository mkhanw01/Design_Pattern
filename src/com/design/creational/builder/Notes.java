package com.design.creational.builder;

import lombok.Data;

/**
 * Created by khan on 3/9/18.
 */
@Data
public class Notes {
  /**         use
   * Separate construction with internal user.
   * One process :  Multiple object representation.
   * Object construction <> object assembling .
   * One build process for multiple similar projects.
   */
  /**
   * Will solve one problem where one application will connect with different databases and.
   * based on query will call to associated database.
   *  will make one interface and will construct there objects.
   */
}
